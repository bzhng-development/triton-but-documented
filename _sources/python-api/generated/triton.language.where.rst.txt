.. py:function:: triton.language.where(condition, x, y, _semantic=None)

   Returns a tensor of elements from either ``x`` or ``y``, depending on ``condition``.

   Note that ``x`` and ``y`` are always evaluated regardless of the value of ``condition``.

   If you want to avoid unintended memory operations, use the ``mask`` arguments in :py:func:`triton.language.load` and :py:func:`triton.language.store` instead.

   The shape of ``x`` and ``y`` are both broadcast to the shape of ``condition``. ``x`` and ``y`` must have the same data type.

   Parameters
   ----------
   condition : Block of triton.bool
       When True (nonzero), yield ``x``, otherwise yield ``y``.
   x : Block
       Values selected at indices where ``condition`` is True.
   y : Block
       Values selected at indices where ``condition`` is False.

   Returns
   -------
   tensor
       A tensor containing elements from ``x`` or ``y`` based on ``condition``.

   Examples
   --------
   .. code-block:: python

      import triton
      import triton.language as tl

      @triton.jit
      def kernel(X_ptr, Y_ptr, Out_ptr, BLOCK_SIZE: tl.constexpr):
          pid = tl.program_id(0)
          offset = pid * BLOCK_SIZE + tl.arange(0, BLOCK_SIZE)
          x = tl.load(X_ptr + offset)
          y = tl.load(Y_ptr + offset)
          # Select x if x > y, else y
          condition = x > y
          out = tl.where(condition, x, y)
          tl.store(Out_ptr + offset, out)